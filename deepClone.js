/**
* @method deepClone()
* @param any 

* takes an object and creates a copy of it

* @param {Object} obj1
* @return {Object} obj2

* tested 24/09/17:
    works in Edge, Firefox, IE and Chrome
*/

  function deepClone(obj1) {
    var obj2 = obj1;
    return (obj2);
  }
